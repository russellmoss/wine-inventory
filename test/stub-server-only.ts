// Test stub for the `server-only` package, which otherwise throws when imported
// outside a React Server Component build. Aliased in vitest.config.ts so pure
// logic in server-only modules (confirm, scope, resolve) can be unit-tested.
export {};
