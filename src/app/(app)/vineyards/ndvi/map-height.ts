/**
 * Map Explorer canvas heights, in one place so the main map and the two compare maps stay in
 * proportion. Raised ~30% (420→546, 340→442) — the layer key now lives ON the map, so the canvas
 * has to carry the controls as well as the imagery.
 */
export const MAP_EXPLORER_MAP_HEIGHT = 546;
export const MAP_EXPLORER_COMPARE_HEIGHT = 442;
